import CustomStyles from "../CustomStyles";

const OrderButton = (): JSX.Element => {
  return (
    <>
      <div className={CustomStyles.notesTypeButtons.order}>Order</div>
    </>
  );
};

export default OrderButton;
