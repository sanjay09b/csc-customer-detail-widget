import React from "react";
import CustomStyles from "../CustomStyles";

const ActiveStatusButton = (): JSX.Element => {
  return (
    <>
      <div className={CustomStyles.statusButtons.active}>Active</div>
    </>
  );
};

export default ActiveStatusButton;
