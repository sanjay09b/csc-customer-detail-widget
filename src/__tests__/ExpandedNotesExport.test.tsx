import { render } from "@testing-library/react";
import React from "react";
import ExpandedNotesExport from "../widget//ExpandedTable/ExpandedNotesExport";

describe("ExpandNotesExport", () => {
  it("renders", () => {
    render(<ExpandedNotesExport />);
  });
});
